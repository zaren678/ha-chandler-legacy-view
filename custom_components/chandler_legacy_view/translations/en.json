{
  "config": {
    "step": {
      "user": {
        "title": "Chandler Legacy View",
        "description": "Set up discovery of Chandler Legacy water system valves.",
        "data": {
          "default_passcode": "Default valve passcode"
        }
      }
    },
    "error": {
      "invalid_passcode": "Passcode must be exactly four digits."
    },
    "abort": {
      "single_instance_allowed": "Only one Chandler Legacy View configuration is permitted."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Chandler Legacy View options",
        "description": "Configure valve access and dashboard recovery behavior.",
        "data": {
          "default_passcode": "Default valve passcode",
          "watchdog_timeout_minutes": "Recovery timeout",
          "device_address": "Valve to configure",
          "device_passcode": "Override passcode",
          "remove_override": "Remove override"
        }
      }
    },
    "error": {
      "device_required": "Select a valve before changing overrides.",
      "invalid_passcode": "Passcode must be exactly four digits.",
      "passcode_not_expected": "Clear the passcode field before removing an override.",
      "passcode_required": "Enter a passcode or choose Remove override to clear it."
    }
  }
}
